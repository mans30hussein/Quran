import 'package:flutter/material.dart';

/// Noor home UI tokens — sourced from Figma node `1:2` (Task 0 audit).
abstract final class AppColors {
  // ── Surfaces ─────────────────────────────────────────
  static const screenBackground = Color(0xFF0D2917);
  static const cardSurface = Color(0xFF122E1A);
  static const cardSurfaceElevated = Color(0xFF1A3D21);

  // ── Program card variants ────────────────────────────
  static const programGreen = Color(0xFF122E1A);
  static const programBlue = Color(0xFF0F2452);
  static const programPurple = Color(0xFF2E0F36);
  static const programOlive = Color(0xFF1A2E0F);

  // ── Text ─────────────────────────────────────────────
  static const textPrimary = Color(0xFFE8F5E0);
  static const textMuted = Color(0xFF7A9E7D);
  static const textAccentGreen = Color(0xFFA3C98C);
  static const accentGold = Color(0xFFF0E06B);
  static const textOnGold = Color(0xFF122E1A);

  // ── Semantic ─────────────────────────────────────────
  static const notification = Color(0xFFE05252);
}
