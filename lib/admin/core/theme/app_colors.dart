import 'package:flutter/material.dart';

abstract class AppColors {
  // ── Primary (Forest Green) ────────────────────────────
  static const primary = Color(0xff0f2318); //0xFF1B4332
  static const primaryLight = Color(0xFF2D6A4F);
  static const primarySurface = Color(0xFFD8F3DC);

  // ── Accent (Islamic Gold) ─────────────────────────────
  static const gold = Color(0xFFB7950B);
  static const goldLight = Color(0xFFF9E79F);
  static const goldSurface = Color(0xFFFEFDE8);

  // ── Backgrounds ──────────────────────────────────────
  static const scaffold = Color(0xFFF5F0E8);
  static const surface = Color(0xFFFAF7F2);
  static const surfaceAlt = Color(0xFFEDE5D0);

  // ── Semantic ─────────────────────────────────────────
  static const info = Color(0xFF1A5276);
  static const infoLight = Color(0xFFD6EAF8);
  static const error = Color(0xFF922B21);
  static const errorLight = Color(0xFFFADBD8);

  // ── Text ─────────────────────────────────────────────
  static const textPrimary = Color(0xFF2C3E50);
  static const textSecondary = Color(0xFF616A6B);
  static const textDisabled = Color(0xFFABB2B9);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const textOnGold = Color(0xFF7D6608);

  // ── Border ───────────────────────────────────────────
  static const border = Color(0xFFD5D8DC);
  static const borderFocus = Color(0xFF2D6A4F);

  // ── Divider ──────────────────────────────────────────
  static const divider = Color(0xFFEDE5D0);
}
