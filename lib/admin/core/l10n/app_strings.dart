abstract class AppStrings {


  // ── Screen ───────────────────────────────────────────
  static const screenTitle          = 'Add Event';
  static const programTitle         = 'Programs';
  // ── Section headers ──────────────────────────────────
  static const sectionBasicInfo     = 'Event Details';
  static const sectionMeeting       = 'Meeting';
  static const sectionSchedule      = 'Schedule';

  // ── Field labels ─────────────────────────────────────
  static const labelEventTitle      = 'EVENT TITLE';
  static const labelDescription     = 'DESCRIPTION';
  static const labelZoomLink        = 'ZOOM LINK';
  static const labelDate            = 'DATE';
  static const labelTime            = 'TIME';
  static const labelDuration        = 'DURATION';

  // ── Hints ────────────────────────────────────────────
  static const hintEventTitle       = 'Event title...';
  static const hintDescription      = 'Describe what this event covers...';
  static const hintZoomLink         = 'https://zoom.us/j/...';
  static const hintPickDate         = 'Pick a date';
  static const hintPickTime         = 'Pick a time';

  // ── Button ───────────────────────────────────────────
  static const btnSaveEvent         = 'Save Event';

  // ── Validation ───────────────────────────────────────
  static const validationFillAll    = 'Please fill in all fields';
}
